# URL do banco de dados (SQLite por padrão, mas pode ser alterado para PostgreSQL, MySQL, etc.)
DATABASE_URI=sqlite:///scheduler.db

# Chave secreta para segurança da sessão (altere em produção!)
SECRET_KEY=dev-key-for-development-only 